package P2098_외판원순회;

import java.io.*;
import java.util.*;

public class Main {
    static int N, endCondition, visit;
    static int[][] W;
    public static void main(String[] args) throws Exception{
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st;
        N = Integer.parseInt(br.readLine());
        W = new int[N+1][N+1];

        endCondition = (int) (Math.pow(2, N)-1);

        for(int i=1; i<=N; i++){
            st = new StringTokenizer(br.readLine());

        }

    }
}
